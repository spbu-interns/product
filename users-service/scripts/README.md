# 📁 Папка scripts/

Эта папка содержит вспомогательные скрипты, тесты и документацию для работы с Users Service.

## 🚀 Скрипты запуска

### `run-server.sh` (macOS/Linux)
Автоматически запускает весь сервис:
- PostgreSQL в Docker
- Виртуальное окружение Python
- Установка зависимостей
- API на http://localhost:8001

**⚠️ Запускать из корня users-service:**
```bash
cd /path/to/users-service
./scripts/run-server.sh
```

### `run-server.ps1` (Windows)
То же самое для Windows PowerShell.

**⚠️ Запускать из корня users-service:**
```powershell
cd C:\path\to\users-service
.\scripts\run-server.ps1
```

### `stop-server.sh` (macOS/Linux)
Останавливает PostgreSQL и сохраняет данные.

**⚠️ Запускать из корня users-service:**
```bash
cd /path/to/users-service
./scripts/stop-server.sh
```

## 🧪 Тестовые скрипты

### `test_avatars.py`
Полный набор Python-тестов для системы аватарок:
- Загрузка обычной аватарки
- Загрузка большого изображения (автоматическое сжатие)
- Проверка неверных форматов
- Получение аватарки
- Удаление аватарки

**Требования:** API должен быть запущен на http://localhost:8001

**Использование:**
```bash
python scripts/test_avatars.py
```

### `test_avatars.sh` (macOS/Linux)
Быстрые bash-тесты для проверки основной функциональности.

**Использование:**
```bash
./scripts/test_avatars.sh
```

## 📖 Документация

### `QUICK_START.md`
Полное руководство по запуску сервиса:
- Пошаговые инструкции для macOS/Linux/Windows
- Команды Docker
- Troubleshooting
- Полезные команды

### `AVATARS_README.md`
Подробная документация системы хранения аватарок:
- Архитектура хранения
- API endpoints
- Примеры использования (Python, JavaScript, curl)
- Настройка безопасности
- Миграция данных

## 📂 Структура

```
scripts/
├── README.md              # Этот файл
├── run-server.sh          # Запуск (macOS/Linux)
├── run-server.ps1         # Запуск (Windows)
├── stop-server.sh         # Остановка
├── test_avatars.py        # Python тесты
├── test_avatars.sh        # Bash тесты
├── QUICK_START.md         # Руководство по запуску
└── AVATARS_README.md      # Документация по аватаркам
```

## 🎯 Быстрые команды

**⚠️ Все команды запускаются из корня `users-service`:**

```bash
# Перейти в корень проекта
cd /Users/tsagoll/StudioProjects/product/users-service

# Запустить всё
./scripts/run-server.sh

# Протестировать аватарки (Python)
python scripts/test_avatars.py

# Протестировать аватарки (bash)
./scripts/test_avatars.sh

# Остановить сервис
./scripts/stop-server.sh
```

## 💡 Советы

1. **Первый запуск:** Используйте `run-server.sh` — он всё настроит автоматически
2. **Для разработки:** API работает в режиме `--reload`, изменения применяются автоматически
3. **Тесты:** Запускайте после каждого изменения API для проверки работоспособности
4. **Документация:** Начните с `QUICK_START.md` для полного понимания процесса
