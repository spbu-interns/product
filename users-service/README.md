# Users Service

> 📖 **[Полное руководство по запуску](scripts/QUICK_START.md)** — все команды в одном месте

## 🚀 Быстрый старт

### Вариант 1: Первый запуск / Полный сброс БД

```bash
./scripts/run-server.sh --reset
```

**Флаг `--reset` делает:**
1. Останавливает и удаляет старую БД (`docker compose down -v`)
2. Запускает PostgreSQL
3. Применяет все SQL миграции (`001-017`)
4. Создаёт тестовые данные:
   - **3 клиники** (Здоровье+, МедСервис, ПроМед)
   - **1 админа** (admin@test.com)
   - **5 докторов** (терапевт, кардиолог, невролог, педиатр, стоматолог)
   - **6 клиентов** с полными профилями
5. Запускает API на http://localhost:8001

**Пароль для всех:** `password123`

После запуска API, в **другом терминале** загрузите аватарки:
```bash
python3 scripts/upload_avatars_via_api.py
```

### Вариант 2: Обычный запуск

Если БД уже создана:

```bash
./scripts/run-server.sh
```

Скрипт автоматически:
- Запустит PostgreSQL (если не запущен)
- При первом запуске применит все миграции
- При последующих запусках обновит схему (без повторного заливания тестовых данных)
- Активирует виртуальное окружение
- Установит зависимости
- Запустит API на http://localhost:8001

---

## 🛑 Остановка сервера

```bash
# Остановить API (Ctrl+C в терминале где запущен)

# Остановить PostgreSQL
./scripts/stop-server.sh
# или
docker compose down
```

---

## 📊 Какой вариант выбрать?

| Ситуация | Команда |
|----------|---------|
| **Первый запуск проекта** | `./scripts/run-server.sh --reset` → `python3 scripts/upload_avatars_via_api.py` |
| **Сломалась БД, нужно пересоздать** | `./scripts/run-server.sh --reset` → `python3 scripts/upload_avatars_via_api.py` |
| **Обычный запуск (БД уже есть)** | `./scripts/run-server.sh` |

---

### Ручной запуск (для продвинутых)

1. **Клонировать проект**
```bash
git clone <repo-url>
cd users-service
```

2. **Поднять базу данных (Postgres в Docker)**
```bash
docker compose up -d
```

- сервис Postgres будет доступен на localhost:5432
- база данных: usersdb
- логин/пароль: app/secret (см. .env)

Проверить контейнер:
```bash
docker compose ps
```

3. **Настроить окружение Python**
```bash
python3 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

4. **Запустить API**
```bash
uvicorn app.main:app --reload --port 8001
```

API будет доступен на:
- Swagger UI → http://127.0.0.1:8001/docs
- ReDoc → http://127.0.0.1:8001/redoc
- Health check → http://127.0.0.1:8001/health

⸻

📂 Структура проекта
```bash
users-service/
├─ docker-compose.yml       # Postgres в контейнере
├─ .env                     # настройки подключения к БД
├─ requirements.txt         # зависимости Python
├─ README.md                # документация
├─ sql/                     # схемы и миграции БД
│  ├─ 001_schema.sql
│  ├─ 002_seed.sql
│  └─ ...
├─ app/                     # Python-код
│  ├─ main.py               # FastAPI-приложение
│  ├─ db.py                 # подключение к БД
│  ├─ models.py             # Pydantic-модели
│  └─ repository.py         # SQL-запросы
├─ avatars/                 # 🆕 Файлы аватарок пользователей
│  └─ .gitkeep
└─ scripts/                 # 🆕 Скрипты и документация
   ├─ run-server.sh         # Быстрый запуск
   ├─ stop-server.sh        # Остановка
   ├─ test_avatars.py       # Тесты
   ├─ QUICK_START.md        # Полное руководство
   └─ AVATARS_README.md     # Документация по аватаркам
```

⸻

🛠 Методы API
```
	•	GET /users/by-email/{email} → найти пользователя по email
	•	GET /users/by-login/{login} → найти пользователя по login
	•	GET /users/exists/email/{email} → проверить существование email
	•	GET /users/exists/login/{login} → проверить существование login
	•	POST /users → добавить нового пользователя

{
  "email": "new@ex.com",
  "login": "newbee",
  "password": "s3cret",
  "role": "CLIENT"
}


	•	GET /users → получить всех пользователей
	•	GET /users?role=CLIENT → получить всех клиентов
```
⸻

 Остановка сервисов

**Автоматически:**
```bash
./scripts/stop-server.sh
```

**Вручную:**
- FastAPI (uvicorn): `Ctrl+C`
- Postgres (Docker):
```bash
docker compose stop        # остановить, данные сохранятся
docker compose down        # остановить и удалить контейнеры
docker compose down -v     # остановить и удалить контейнеры + данные
```
## 🔄 Обновление схемы БД после правок в `sql/001_schema.sql` и применение `sql/004_email_tokens.sql`, `sql/005_patient_complaints_notes.sql`
### Способ 1 — **снос данных и полная инициализация заново** (простое, **удаляет все данные**)
```bash
docker compose down -v
```

```bash
docker compose up -d
```

## 📸 Работа с аватарками

Аватарки пользователей хранятся как файлы в папке `avatars/`. См. подробную документацию: [scripts/AVATARS_README.md](scripts/AVATARS_README.md)

### Быстрый старт:

```bash
# Загрузить аватарку
curl -X POST "http://localhost:8001/users/1/avatar" \
  -F "file=@avatar.jpg"

# Получить аватарку
curl "http://localhost:8001/users/1/avatar" --output avatar.jpg

# Удалить аватарку
curl -X DELETE "http://localhost:8001/users/1/avatar"
```

**Поддерживаемые форматы:** JPG, PNG, WebP, GIF  
**Максимальный размер:** 5 MB  
**Автоматическая оптимизация:** ✅

### ⚠️
- Пароли хранятся как bcrypt-хэши (не в открытом виде).
- Начальные данные загружаются из sql/002_seed.sql.
